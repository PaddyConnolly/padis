[package]
name = "padis"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1", features = ["full"] }
mini-redis = "0.4"
bytes = "1"
thiserror = "1"
atoi = "2.0.0"

[dev-dependencies]
tokio-test = "0.4"
redis = { version = "1.0.2", features = ["tokio-comp"] }
